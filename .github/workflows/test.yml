name: Test Features

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  validate:
    name: Validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Validate JSON schema
        run: |
          for file in src/*/devcontainer-feature.json; do
            echo "Validating $file"
            jq empty "$file"
          done

      - name: Shellcheck
        run: |
          sudo apt-get update && sudo apt-get install -y shellcheck
          for script in src/*/install.sh src/*/test/test.sh scripts/*.sh; do
            if [[ -f "$script" ]]; then
              echo "Checking $script"
              shellcheck "$script"
            fi
          done

  test:
    name: Test Features
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Set up Docker
        uses: docker/setup-buildx-action@v4

      - name: Build test image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./test/Dockerfile.test
          push: false
          load: true
          tags: devcontainer-features:test

      - name: Test default install (core tools only)
        run: |
          docker run \
            --rm \
            devcontainer-features:test \
            bash -c "
              set -euo pipefail
              echo '=== Running feature install.sh (defaults) ==='
              cd src/neovim-pack
              bash install.sh
              echo ''
              echo '=== Running feature tests (defaults) ==='
              bash test/test.sh
            "

      - name: Test with optional tools (pip + node)
        run: |
          docker run \
            --rm \
            -e INSTALLPIP=true \
            -e INSTALLNODE=true \
            devcontainer-features:test \
            bash -c "
              set -euo pipefail
              echo '=== Running feature install.sh (pip + node) ==='
              cd src/neovim-pack
              bash install.sh
              echo ''
              echo '=== Running feature tests (pip + node) ==='
              bash test/test.sh
            "
